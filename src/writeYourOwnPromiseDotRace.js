var Promise = require('bluebird');

/**
 * Write your own version of `Promise.race`
 *
 * Return a promise that is fulfilled or rejected
 * as soon as a promise in the array is fulfilled or rejected
 * with the respective rejection reason or fulfillment value.
 */

var promiseDotRace = function (arrayOfPromises) {
  // YOUR CODE HERE
};

module.exports = promiseDotRace;
